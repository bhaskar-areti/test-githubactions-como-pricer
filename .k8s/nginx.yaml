apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-test06-May-26
  namespace: gha-test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx-test06-May-26
  template:
    metadata:
      labels:
        app: nginx-test06-May-26
    spec:
      containers:
        - name: nginx
          image: nginx:1.25