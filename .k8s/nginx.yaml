apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-test08-may-26-v1a
  namespace: gha-test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx-test08-may-26-v1a
  template:
    metadata:
      labels:
        app: nginx-test08-may-26-v1a
    spec:
      containers:
        - name: nginx
          image: nginx:1.25